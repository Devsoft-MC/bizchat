# iOS simulator and web can use localhost. Android emulator uses 10.0.2.2 by default.
# For a physical phone, replace this with your computer's LAN IP address.
EXPO_PUBLIC_API_URL=http://localhost:5001/api
